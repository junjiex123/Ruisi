<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="xyz.yluo.ruisiapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".PublicData"
        android:allowBackup="true"
        android:icon="@mipmap/logo"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.LaunchActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activity.HomeActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:windowSoftInputMode="adjustPan|stateHidden">

        </activity>

        <activity
            android:name=".activity.ArticleListNormalActivity"
            android:theme="@style/AppTheme">

        </activity>

        <activity
            android:label="种子列表"
            android:name=".activity.ArticleListBtActivity"
            android:theme="@style/AppTheme">

        </activity>

        <activity
            android:name=".activity.ArticleListImageActivity"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name=".activity.AboutActivity"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <activity
            android:name=".activity.SingleArticleActivity"
            android:hardwareAccelerated="true"
            android:theme="@style/AppTheme"
            android:windowSoftInputMode="adjustResize">
        </activity>

        <activity android:name=".activity.SettingActivity"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:name=".activity.UserDetailActivity"
            android:theme="@style/AppTheme.NoActionBar"
            android:label="用户信息">
        </activity>

        <activity android:name=".activity.LoginActivity"
            android:label="登陆"
            android:theme="@style/AppTheme">
        </activity>

        <activity
            android:label="签到中心"
            android:name=".activity.UserDakaActivity"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <activity
            android:name=".activity.ChatActivity"
            android:theme="@style/AppTheme">
        </activity>

        <activity android:name=".activity.NewArticleActivity_2"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>

        <activity android:name=".activity.ActivitySearch"
            android:theme="@style/AppTheme">
        </activity>

        <activity android:name=".activity.ActivityFriend"
            android:label="我的好友"
            android:theme="@style/AppTheme">
        </activity>

        <receiver android:name=".checknet.NetworkReceiver">
            <intent-filter>
                <action android:name="android.net.conn.CONNECTIVITY_CHANGE"/>
            </intent-filter>
        </receiver>

    </application>

</manifest>